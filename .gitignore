CTF writeups/
.DS_Store
**/.DS_Store

# Agent instruction files must never be published from this repo.
# This repo is served by GitHub Pages, so anything committed is world-readable.
# The working copy lives in _local/, which .git/info/exclude keeps out of git.
CLAUDE.md
**/CLAUDE.md
claude.md
**/claude.md
AGENTS.md
**/AGENTS.md
GEMINI.md
**/GEMINI.md
.cursorrules

# Machine-only scratch. Previously excluded by .git/info/exclude alone, which is
# NOT versioned - it does not survive a fresh clone and does not stop `git add -f`.
# This repo is served by GitHub Pages, so anything committed is world-readable.
_local/
**/_local/
.gstack/
**/.gstack/
